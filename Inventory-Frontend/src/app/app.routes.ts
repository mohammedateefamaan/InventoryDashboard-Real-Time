import { Routes } from '@angular/router';
import { InventoryDashboard } from './components/inventory-dashboard/inventory-dashboard';

export const routes: Routes = [{ path: '', component: InventoryDashboard }];
